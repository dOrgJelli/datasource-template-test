{
  "name": "dynamic-datasource-test",
  "version": "0.0.0-alpha",
  "engines": {
    "node": ">=9.4.0"
  },
  "scripts": {
    "start": "cd ./subgraph && npm run start",
    "stopp": "cd ./subgraph && npm run stop:env",
    "postinstall": "npx lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.14.1"
  }
}
